# The Number type in Swish is a general type to express decimals, floats, octals and more.

num = 0 # `0` is the only falsey number value.

num = 110

num = 0xBADa55
num = 0XbADa55

num = 0600

num = 1.0 # Floats as with all other numbers grow as needed, starting as small as 8 bits up to 64 bits.
num = .42e-5 # Floats can have exponents.
